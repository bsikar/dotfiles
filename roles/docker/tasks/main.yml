- name: install docker
  become: yes
  apt:
    name:
      - docker.io
      - docker-compose
  tags:
    - apt
    - docker

- name: add user to docker group
  become: yes
  shell: "usermod -aG docker {{ ansible_env.USER }}"
  tags:
    - docker
