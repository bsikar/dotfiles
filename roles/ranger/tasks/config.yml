- name: symlink dotfiles
  file:
    src: "{{ ansible_env.PWD }}/roles/ranger/files/{{ item }}"
    path: ~/.config/ranger/{{ item }}
    state: link
    follow: false
    force: yes
  loop:
    - rc.conf
  tags:
    - ranger
